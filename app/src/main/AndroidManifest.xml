<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="emil.meyn.dailytasks">
    <!-- reguirement for internet requests -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Test for API connection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoginPage"></activity>
        <activity android:name=".Activities.TaskDetails" />
        <activity android:name=".Activities.ListOfTasks" />
        <activity android:name=".Activities.AddTaskActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirebaseUIActivity"></activity>
    </application>

</manifest>